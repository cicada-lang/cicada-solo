class PreOrder {
  Elem: Type
  Pre(Elem, Elem): Type

  reflexive(a: Elem): Pre(a, a)

  @given(a: Elem, b: Elem, c: Elem)
  transitive(Pre(a, b), Pre(b, c)): Pre(a, c)

  StrictPre(a: Elem, b: Elem): Type = Pre(a, b) & NonEqual(Elem, a, b)
}

// NOTE
// thin category
// hom_set(A, B).size <= 1
