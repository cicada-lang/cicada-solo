class PreOrder {
  Elem: Type
  Pre(Elem, Elem): Type

  reflexive(a: Elem): Pre(a, a)

  transitive(# a b c: Elem, Pre(a, b), Pre(b, c)): Pre(a, c)

  StrictPre(a, b: Elem): Type = Pre(a, b) & NonEqual(Elem, a, b)
}

// NOTE
// thin category
// hom_set(A, B).size <= 1
