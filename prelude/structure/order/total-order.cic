class TotalOrder extends PartialOrder {
  connective : {
    a, b : Elem
    ---------
    either_t(Pre(a, b), Pre(b, a))
  }
}

// NOTE
// can implement
// - quick sort
// - bubble sort
// - merge sort
