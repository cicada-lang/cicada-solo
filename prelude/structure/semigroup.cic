class Semigroup {
  Elem : type
  mul : { x, y : Elem -> Elem }
  mul_associative : {
    x, y, z : Elem
    ---------
    Equation(mul(x, mul(y, z)), mul(mul(x, y), z))
  }
}

class Semigroup {
  Elem : type
  mul : { List(Elem) -> Elem }
}
