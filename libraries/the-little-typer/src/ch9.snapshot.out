the Equal(Nat, 4, 4) same
the Equal(Nat, 1, 1) same
the Equal(Nat, 2, 2) same
the Equal(Nat, 3, 3) same
the Equal(Nat, 4, 4) same
the Nat 2
the Nat 4
the Nat 6
the Nat 8
the Nat 2
the Nat 4
the Nat 6
the Nat 8
the Equal(Nat, 34, 34) same
the Vector(String, 6) vec("a", vec("a", vec("b", vec("b", vec("c", vec("c", vecnil))))))
the Vector(String, 6) vec("a", vec("a", vec("b", vec("b", vec("c", vec("c", vecnil))))))
