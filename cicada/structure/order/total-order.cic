@module structure.order.TotalOrder

@import structure.order.PartialOrder

// NOTE Can implement:
// - quick sort
// - bubble sort
// - merge sort
@class TotalOrder extends PartialOrder {
  connective(x: Elem, y: Elem): Either(Under(x, y), Under(y, x))
}
