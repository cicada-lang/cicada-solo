let add: (Nat, Nat) -> Nat =
  (x, y) => {
    return nat_ind(y, (_) => Nat, x, (prev, almost) => add1(almost))
  }

let my_nat_which: (target: Nat) -> (T: Type) -> (base: T) -> (step: (prev: Nat) -> T) -> T =
  (target, T, base, step) => {
    return nat_ind(target, (_) => T, base, (prev, _almost) => step(prev))
  }

let gauss: (Nat) -> Nat =
  (n) => {
    return my_nat_which(n, Nat, 0, (prev) => add(add1(prev), gauss(prev)))
  }
