develop category_t {
  class initial_t {
    object: object_t
    property(x: object_t): exists(
      f: morphism_t(object, x),
      forall(g: morphism_t(object, x)) -> eqv_t(f, g))
  }

  initial_unique_up_to_unique_isomorphism: TODO = TODO
}
