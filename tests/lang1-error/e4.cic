{
  one = add1(zero) : Nat
  two = add1(one) : Nat
  id = (x) => x : (Nat) -> Nat
  (_) => (_) => id(id(id(id(two)))) : (Nat) -> Nat
  // NOTE should be:
  //   (_) => (_) => id(id(id(id(two)))) : (Nat) -> (Nat) -> Nat
}
