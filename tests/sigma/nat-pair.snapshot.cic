def one = add1(zero)
def two = add1(one)
def three = add1(two)
def four = add1(three)

(Nat) * Nat

def one_two: (Nat) * Nat = cons(one, two)
cdr(one_two)

(Nat, Nat) * Nat

def one_two_three: (Nat, Nat) * Nat = cons(one, cons(two, three))
cdr(cdr(one_two_three))

(Nat, Nat, Nat) * Nat

def one_two_three_four: (Nat, Nat, Nat) * Nat = cons(one, cons(two, cons(three, four)))
cdr(cdr(cdr(one_two_three_four)))
