one = add1(zero)
two = add1(one)
three = add1(two)
four = add1(three)

(Nat) * Nat

one_two: (Nat) * Nat = cons(one, two)
cdr(one_two)

(Nat, Nat) * Nat

one_two_three: (Nat, Nat) * Nat = cons(one, cons(two, three))
cdr(cdr(one_two_three))

(Nat, Nat, Nat) * Nat

one_two_three_four: (Nat, Nat, Nat) * Nat = cons(one, cons(two, cons(three, four)))
cdr(cdr(cdr(one_two_three_four)))
