export class FixedPi {
  // NOTE In check-mode, implicit arguments is called "fixed",
  //   which decorates on top of pi type,
  //   and will be resolved from return type,
  //   making it can not be used in infer-mode.
}
