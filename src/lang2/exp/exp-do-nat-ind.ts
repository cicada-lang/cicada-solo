import * as Exp from "../exp"
import * as Value from "../value"

export function do_nat_ind(
  target: Value.Value,
  motive: Value.Value,
  base: Value.Value,
  step: Value.Value
): Value.Value {
  throw new Error()
}
